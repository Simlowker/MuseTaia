apiVersion: v1
kind: Secret
metadata:
  name: smos-secrets
type: Opaque
stringData:
  # IMPORTANT: These are placeholders. Use Google Secret Manager or K8s External Secrets for production.
  # The keys previously here were compromised and must be revoked.
  GOOGLE_API_KEY: "REPLACE_WITH_SECURE_KEY"
  GOOGLE_OAUTH_CLIENT_ID: "REPLACE_WITH_CLIENT_ID"
  GOOGLE_OAUTH_CLIENT_SECRET: "REPLACE_WITH_CLIENT_SECRET"
  APIFY_TOKEN: "REPLACE_WITH_APIFY_TOKEN"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: smos-config
data:
  PROJECT_ID: "muse-taia"
  PROJECT_NUMBER: "289248039846"
  LOCATION: "us-central1"
  GCS_BUCKET_NAME: "smos-assets"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  COMFYUI_SERVER_ADDRESS: "comfyui-service:8188"