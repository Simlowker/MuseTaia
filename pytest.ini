[pytest]
markers =
    integration: Tests requiring real services (Vertex AI, Redis, GCS)
    slow: Tests that take longer than 10s
    expensive: Tests that consume significant API credits
    
testpaths = tests
python_files = test_*.py
python_functions = test_*

# By default, exclude integration tests
addopts = -m "not integration"
asyncio_mode = auto
